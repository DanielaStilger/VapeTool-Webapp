import { createStyles } from 'antd-style'

export default createStyles(() => ({
  pre: {
    margin: '12px 0',
    padding: '12px 20px',
    background: '@input-bg',
    boxShadow: '@card-shadow'
  }
}))
